class Solution:
    def search(self, nums: List[int], target: int) -> int:
        
        #hashtable
        if target not in nums:
            return -1

        
        for i in range(len(nums)):
            
            
            if nums[i] == target:
                return i
                
            
            else:
                i+=1
 #stupid method above
